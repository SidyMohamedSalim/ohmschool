// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model SuperAdmin {
  idSuperAdmin String  @id @default(cuid())
  name         String
  email        String?
  username     String?
}
